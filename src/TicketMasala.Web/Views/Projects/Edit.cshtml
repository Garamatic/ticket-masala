@model TicketMasala.Web.ViewModels.Projects.NewProject

@{
    ViewData["Title"] = "Edit Project";
}

<h1>@ViewData["Title"]</h1>

<div class="row">
    <div class="col-md-6">
        <form asp-action="Edit" asp-route-id="@Model.Guid" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <input type="hidden" asp-for="Guid" />
            
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="CreationDate" class="control-label">Completion Target</label>
                <input asp-for="CreationDate" type="date" class="form-control" />
                <span asp-validation-for="CreationDate" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="CreationDate" class="control-label">Completion Target</label>
                <input asp-for="CreationDate" type="date" class="form-control" />
                <span asp-validation-for="CreationDate" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="ProjectType" class="form-label">Project Type</label>
                <select asp-for="ProjectType" class="form-select">
                    <option value="">-- Select Project Type --</option>
                    <option value="FullRenovation">Full Renovation</option>
                    <option value="PartialRenovation">Partial Renovation</option>
                    <option value="CleanUp">Clean Up</option>
                    <option value="Modified">Modified</option>
                </select>
                <span asp-validation-for="ProjectType" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="SelectedCustomerId" class="control-label">Customer</label>
                <select asp-for="SelectedCustomerId" asp-items="@Model.CustomerList" class="form-control">
                    <option value="">-- Select a customer --</option>
                </select>
                <span asp-validation-for="SelectedCustomerId" class="text-danger"></span>
            </div>
            
            <div class="form-group mb-3">
                <label asp-for="SelectedProjectManagerId" class="control-label">Project Manager</label>
                <select asp-for="SelectedProjectManagerId" asp-items="@Model.ProjectManagerList" class="form-control">
                    <option value="">-- Select Project Manager --</option>
                </select>
                <span asp-validation-for="SelectedProjectManagerId" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="ProjectComment" class="form-label">Comments (Notes)</label>
                <textarea asp-for="ProjectComment" class="form-control" rows="3"></textarea>
                <span asp-validation-for="ProjectComment" class="text-danger"></span>
            </div>

            <div class="form-group mt-3">
                <button type="submit" class="btn btn-primary">Save Changes</button>
                <a asp-action="Details" asp-route-id="@Model.Guid" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

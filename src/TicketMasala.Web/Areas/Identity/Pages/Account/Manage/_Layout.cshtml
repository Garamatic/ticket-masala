@using Microsoft.Extensions.Localization
@using TicketMasala.Web.Resources
@inject IStringLocalizer<SharedResource> Localizer
@{
    var defaultLayout = "/Areas/Identity/Pages/_Layout.cshtml";
    if (ViewData.TryGetValue("ParentLayout", out var parentLayout) && parentLayout !=  null)
    {
        Layout = parentLayout as string ?? parentLayout.ToString();
    }
    else
    {
        Layout = defaultLayout;
    }
}

<h1>@Localizer["Manage your account"]</h1>

<div>
    <h2>@Localizer["Change your account settings"]</h2>
    <hr />
    <div class="row">
        <div class="col-md-3">
            <partial name="_ManageNav" />
        </div>
        <div class="col-md-9">
            @RenderBody()
        </div>
    </div>
</div>

@section Scripts {
    @RenderSection("Scripts", required: false)
}

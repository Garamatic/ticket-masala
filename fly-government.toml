# fly.toml for Government Services Tenant
app = 'ticket-masala-government'
primary_region = 'ord'

[build]
  dockerfile = 'Dockerfile'

[env]
  ASPNETCORE_ENVIRONMENT = 'Production'
  ASPNETCORE_URLS = 'http://+:8080'
  MASALA_CONFIG_PATH = '/app/tenants/government/config'
  ConnectionStrings__DefaultConnection = 'Data Source=/app/tenants/government/data/masala.db;Cache=Shared;Mode=ReadWriteCreate'

[[mounts]]
  source = 'government_data'
  destination = '/app/tenants/government/data'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

  [[http_service.checks]]
    interval = '15s'
    timeout = '2s'
    grace_period = '30s'
    path = '/health'

[[vm]]
  memory = '2gb'
  cpu_kind = 'shared'
  cpus = 1
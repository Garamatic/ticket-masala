app = "ticket-masala-api-hennessey"
primary_region = "ams"

[build]
dockerfile = "Dockerfile"

[env]
ASPNETCORE_ENVIRONMENT = "Production"
MASALA_TENANT_ID = "hennessey"
MASALA_DB_PATH = "/data/masala.db"

[mounts]
source = "masala_data_hennessey"
destination = "/data"

[http_service]
internal_port = 8080
force_https = true
auto_stop_machines = "stop"
auto_start_machines = true
min_machines_running = 0
processes = ["app"]

[[vm]]
memory = "512mb"
cpu_kind = "shared"
cpus = 1
